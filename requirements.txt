# Versions are pinned to the known-good set the app was built and tested against.
# When upgrading, bump one line, run the full test suite, and verify recording +
# transcription on real hardware before committing.

# Phase 1 — Audio Capture
pyaudiowpatch==0.2.12.8   # WASAPI loopback support on Windows (replaces sounddevice for this use case)
# stdlib: wave, subprocess, tempfile, threading — no extra installs needed
# system dep: ffmpeg must be in PATH (for mp3 encoding and mixing)

# Phase 2 — Job Queue (no extra deps, uses built-in sqlite3)

# Phase 3 — Transcription
faster-whisper==1.2.1

# Phase 4 — Note Generation
anthropic==0.84.0
openai==2.36.0
mistralai==2.4.5
keyring==25.7.0

# Phase 5 — UI & Tray
flask==3.1.3
pystray==0.19.5
Pillow==12.2.0
pywebview==6.2.1

# --- Transitive dependencies, pinned for reproducible builds -----------------
# Previously left unpinned, so ctranslate2/onnxruntime/numpy/av (faster-whisper's
# heaviest transitive deps, all bundled into the PyInstaller build) silently
# drifted to whatever was latest-compatible at install time. That drift caused
# the actual v1.0.0 release installer and a later local rebuild to differ by
# ~50MB for no code reason, and is exactly the kind of silent difference that
# should never happen between two builds of the same source. Full resolved
# closure of the direct deps above, captured from a clean `pip install -r
# requirements.txt` (validated in CI). When bumping a direct dependency above,
# regenerate this block by diffing `pip freeze` in a fresh venv.
annotated-types==0.7.0
anyio==4.14.1
av==18.0.0
blinker==1.9.0
bottle==0.13.4
certifi==2026.6.17
cffi==2.1.0
clr_loader==0.3.1
ctranslate2==4.8.1
distro==1.9.0
docstring-parser==0.18.0
eval-type-backport==0.4.0
filelock==3.29.7
flatbuffers==25.12.19
fsspec==2026.6.0
h11==0.16.0
hf-xet==1.5.1
httpcore==1.0.9
httpx==0.28.1
huggingface-hub==1.23.0
idna==3.18
importlib-metadata==8.7.1
itsdangerous==2.2.0
jaraco.classes==3.4.0
jaraco.context==6.1.2
jaraco.functools==4.5.0
jinja2==3.1.6
jiter==0.16.0
jsonpath-python==1.1.6
markupsafe==3.0.3
more-itertools==11.1.0
numpy==2.5.1
onnxruntime==1.27.0
opentelemetry-api==1.39.1
opentelemetry-semantic-conventions==0.60b1
protobuf==7.35.1
proxy_tools==0.1.0
pycparser==3.0
pydantic==2.13.4
pydantic-core==2.46.4
python-dateutil==2.9.0.post0
pythonnet==3.1.0
pywin32-ctypes==0.2.3
pyyaml==6.0.3
setuptools==83.0.0
six==1.17.0
sniffio==1.3.1
tokenizers==0.23.1
tqdm==4.68.4
typing-extensions==4.16.0
typing-inspection==0.4.2
werkzeug==3.1.8
zipp==4.1.0
